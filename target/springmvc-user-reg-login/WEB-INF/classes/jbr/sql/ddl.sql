DROP TABLE IF EXISTS BASIC_USERINFO;

CREATE TABLE BASIC_USERINFO (
            USER_NAME VARCHAR(250) NOT NULL,
            PASS_WORD VARCHAR(250) NOT NULL,
            EMAIL VARCHAR(250) NOT NULL,
            AGE INT(20) NOT NULL,
            GENDER VARCHAR(250) NOT NULL
);
